// Include the most common headers from the C standard library
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Include the main libnx system header, for Switch development
#include <switch.h>

// Include main rust file
#include <libswt.h>

// Main program entrypoint
int main(int argc, char *argv[]) {
    run();
    return 0;
}
